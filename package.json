{
  "name": "serverless-nuxt",
  "version": "1.0.0",
  "description": "Nuxt.js with Serverless",
  "author": "",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build && backpack build",
    "start": "nuxt build && backpack build && serverless offline start",
    "deploy": "nuxt build && backpack build && serverless deploy",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "aws-serverless-express": "^3.0.2",
    "compression": "^1.7.0",
    "express": "^4.15.4",
    "nuxt": "^1.4.0",
    "vuex-class": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^9.6.1",
    "babel-eslint": "^7.2.3",
    "backpack-core": "^0.4.2",
    "nuxt-property-decorator": "^1.0.0",
    "serverless": "^1.20.2",
    "serverless-offline": "^3.15.3",
    "ts-loader": "^3.5.0",
    "typescript": "^2.8.1"
  }
}
